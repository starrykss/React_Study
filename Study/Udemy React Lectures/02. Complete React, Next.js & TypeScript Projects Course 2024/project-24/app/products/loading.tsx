'use client';

import LoadingContainer from '@/components/global/LoadingContainer';

const loading = () => {
  return (
    <div>
      <LoadingContainer />
    </div>
  );
};

export default loading;
